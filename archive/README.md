# archive/

已经被主包 `toivo/` 取代的旧代码 / 旧模板, 保留只为回溯参考。**不要 import**、**不要修改**。

- `v1_original/` — V1 时期的原生 scripts / prompts / templates / static, 现在的 `toivo/products/career_resume/` 是它的重构后版本。

如果要清理磁盘, 这整个目录可以整体删除。
