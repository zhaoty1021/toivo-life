# Anthropic API 直连时用 (走 --provider anthropic)
# 在 Claude Code 里跑 (--provider claude_code) 就不用配这项。
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 可选: 自建代理/中转 URL
# ANTHROPIC_BASE_URL=https://your-proxy.example.com

# 可选: 默认 provider (不设就用 anthropic)
# TOIVO_DEFAULT_PROVIDER=anthropic
